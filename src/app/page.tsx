import Sidebar from "@/components/layouts/sidebar";

const Page = () => {
  return (
    <div>
      <Sidebar />
      <button type="button" className="btn">
        Daisy UI Button
      </button>
    </div>
  );
};

export default Page;
